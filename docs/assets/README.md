# Assets

Place static assets here:

- **`CDF_NAC_Protocol_v2.02.pdf`** — the canonical source PDF, linkable as a
  downloadable artifact. (To be added by the authors.)
- Images, diagrams, and other media referenced by the documentation pages.

> Long-term goal: the Markdown files become the single source of truth and the
> PDF is *generated* from them (e.g. via a MkDocs PDF export plugin), rather
> than the PDF being the master.
